# TECHNICAL ARCHITECTURE

The canonical description of Noisy's engine — the layer we preserve. This document is kept **accurate to the code**. When code and this document disagree, that is drift to be resolved by a ruling recorded in `DECISIONS_AND_CHANGELOG.md`.

**Last updated:** July 16, 2026
**Split from:** `noisy_design_document.md` v2.4 (archived), corrected against `docs/audits/AUDIT_REPORT.md`
**Scope:** engine services, data model, audio behavior. Product/UX lives in `PRODUCT_DESIGN.md`.

> **⚠ Known structural risk — background audio (see Decision D-004).**
> The journey and motif engines are driven by Dart `Timer`s. Dart timers are throttled or suspended when the app is backgrounded or the screen locks. The app currently has **no** background-audio support (no `audio_session`/`audio_service`, no iOS `UIBackgroundModes`, no Android foreground service). For an app whose core use cases are multi-minute sleep and focus sessions, this is both a launch blocker *and* a potential threat to the preserved timing model: adding background support may require reworking the `Timer`-based journey/motif scheduling around a clock- or position-based approach. This risk is recorded here so it stays visible while the engine is treated as canonical. Not yet actioned; see `ROADMAP.md` Phase 4.

---

## 1. Tech Stack

- **Framework:** Flutter 3.44.6, Dart 3.12.2 *(corrected from doc v2.4's 3.41.1 — audit #1)*
- **Audio playback (samples):** just_audio — MP3 playback, crossfade looping, volume fading, motif one-shots
- **Audio synthesis (tones):** flutter_soloud — real-time waveform generation, binaural beats via stereo panning
- **LLM integration:** Anthropic API via `http` + `flutter_dotenv` for key management
  - ⚠ *Security note: `.env` is bundled as a Flutter asset, so the API key ships inside the app binary. This is a pre-launch blocker — the call must move server-side. See `ROADMAP.md` Phase 4.*
- **Backend services:** Firebase — Analytics, Crashlytics, Remote Config
- **Persistence:** `shared_preferences` (local save/load of custom mixes)
- **Testing platform:** Android emulator + real device (primary for audio). Chrome has known `setSpeed`/pitch-shifting artifacts absent on mobile. iOS unvalidated.

**Environment notes:**
- Windows Developer Mode must be enabled for flutter_soloud (symlink requirement)
- flutter_soloud web setup requires two script tags in `web/index.html`
- M4 Mac available for iOS builds via Xcode

---

## 2. File Structure (lib/)

*(Corrected against audit #2 — includes `analytics_service.dart` and `firebase_options.dart`, which doc v2.4 §3 omitted.)*

```
lib/
  main.dart                         — entry point, MainShell, Firebase + Remote Config init, DI
  firebase_options.dart             — Firebase config (generated by flutterfire)
  models/
    journey.dart                    — SoundSource hierarchy, Journey, JourneyWaypoint, SavedMix
    sound_meta.dart                 — kSoundCatalog (43 entries; 35 available)
    mood_profile.dart               — kMoodProfiles affinity map
    motif_meta.dart                 — kMotifCatalog (9 motifs)
    saved_mix.dart                  — MixLayer / SavedMix persistence model
  services/
    audio_engine.dart               — layer lifecycle, crossfade, fades
    tone_service.dart               — flutter_soloud synthesis
    journey_engine.dart             — timeline interpolation
    motif_engine.dart               — generative one-shots
    mood_engine.dart                — selection logic
    harmonic_matcher.dart           — pure pitch math + carrier placement
    llm_service.dart                — natural-language mood parsing
    analytics_service.dart          — Firebase Analytics event wrappers
    storage_service.dart            — shared_preferences save/load
  screens/
    home_screen.dart, mixer_screen.dart, library_screen.dart,
    journey_screen.dart, tone_test_screen.dart
```

---

## 3. Core Systems

### 3.1 Audio Engine (`audio_engine.dart`)

Manages all active audio layers — sample-based (just_audio) and tone-based (flutter_soloud). Extends `ChangeNotifier`.

- Up to **5 simultaneous layers** (samples and tones counted together; motifs are separate and do not count toward the cap)
- Independent volume control per layer
- **Smooth transitions:** `addLayer` starts at 0 and fades in over 1.5s to an optional `volume:` target that **defaults to 0.7** *(audit #4, ruled R-04/D-007 — `addLayer` now mirrors `addToneLayer`/`addBinauralLayer`, which already honor a `volume:` target; with the default in place every existing call site is unchanged, so present behavior is identical. Having the journey engine pass real per-layer targets on add is deferred to V2.)*; `removeLayer` fades to 0 over 1s before disposal
- **Crossfade looping** for sample layers: two-player system with a 3s crossfade window starting 3.5s before the loop point. Uses **equal-power (cos/sin) curves** so perceived loudness stays constant through the transition (sin²+cos²=1, avoiding the ~3dB dip of linear fades)
  - *Perf note (external eval §2 #2): `_startCrossfade` allocates and decodes a fresh `AudioPlayer` every loop cycle of every sample layer.*
- **Tone layers** via ToneService: `addToneLayer()`, `addBinauralLayer()`, with real-time frequency/volume control
- `setPitchShift(assetPath, ratio)` — uses just_audio `setSpeed()` as an approximation (alters tempo+duration with pitch; TODO: migrate to SoLoud for true pitch-shift-without-tempo-change)

**Layer ID conventions:**
- Sample/soundscape layers: asset path (e.g. `assets/audio/soundscapes/open_sky_g_maj.mp3`)
- Tone layers: `tone:528` (prefix + frequency)
- Binaural layers: `binaural:10` (prefix + beat frequency)

**Concurrency:** `removeLayer` re-looks-up the layer index after its fade completes (parallel removals may have shifted positions). UI listeners debounce engine notifications via `addPostFrameCallback` to avoid mid-frame `setState`.

### 3.2 Tone Service (`tone_service.dart`)

Wraps flutter_soloud waveform generation.

- `playTone(frequency, volume)` → tone ID
- `playBinaural(centerFreq, beatFreq, volume)` → two sine waves panned L/R, returns ID
- `setToneFrequency`, `setToneVolume`, `setBinauralVolume`, `setBinauralFrequencies`
- `stopTone(id)`, `stopAll()`

**Binaural mechanism:** one sine panned fully left at `centerFreq`, another panned fully right at `centerFreq + beatFreq`; the brain perceives the difference as a beat. Requires headphones.

### 3.3 Journey Engine (`journey_engine.dart`)

Interpolates between mixer states (waypoints) over a duration. States: stopped, playing, frozen. Has a `_ticking` re-entrancy guard. `abandon()` deliberately does **not** call `motifEngine.stop()`; the next `MotifEngine.start()` awaits `stop()` internally (documented race fix).

### 3.4 Mood Engine (`mood_engine.dart`)

Generates a soundscape mix from an (energy, focus, warmth) input.

**Category inference** (`_inferCategory`) — *(audit #5, ruled R-05: this is the behavior of record; doc v2.4's thresholds matched nothing in the code and assigned the code's Relax region to Meditate. Thresholds are deliberately **not** retuned — the taxonomy is expected to be replaced rather than retuned in V2; see D-010 and `PRODUCT_DESIGN.md` §3.8.)*

A sequence of guards, **first match wins** — order is behavioral, not cosmetic:

| # | Condition | Category |
|---|---|---|
| 1 | `energy < 0.25 && focus < 0.30` | `Sleep` |
| 2 | `focus > 0.65` | `Focus` |
| 3 | `energy > 0.65` | `Energize` |
| 4 | `energy < 0.35 && warmth > 0.50` | `Relax` |
| 5 | `focus > 0.45` | `Meditate` |
| 6 | *(fallthrough)* | `Relax` |

```dart
if (energy < 0.25 && focus < 0.30) return 'Sleep';
if (focus  > 0.65)                  return 'Focus';
if (energy > 0.65)                  return 'Energize';
if (energy < 0.35 && warmth > 0.50) return 'Relax';
if (focus  > 0.45)                  return 'Meditate';
return 'Relax';
```

Consequences of the ordering worth knowing before reasoning about a slider position:
- **`Relax` is reachable two ways** — rule 4 (low energy + warm) and rule 6 (everything unmatched). It is both a specific region *and* the default.
- **`Meditate` is a narrow residue.** It needs `focus` in `(0.45, 0.65]` *and* to have escaped rules 1, 3 and 4 — so warm low-energy inputs become `Relax` (rule 4) before `Meditate` is ever tested.
- **`warmth` participates in exactly one rule** (4). It has no influence anywhere else in category inference.
- **Rules 2 and 3 precede rule 4**, so high `focus` or high `energy` wins over the warmth rule regardless of warmth.

Category drives motif palette, motif density, journey name, and journey icon.

**5-slot selection** (`_selectByCategory`), volumes from Remote Config *(audit #7 — not hardcoded constants)*:
1. **Frequency** resolved first (so it can inform soundscape selection) — `frequency_volume` (default 0.30), skip if distance > 1.2
2. **Soundscape** — `soundscape_volume` (default 0.55); scored **60% mood fit + 40% harmonic compatibility** with the solfeggio (falls back to pure mood distance if no solfeggio in mix)
3. **Nature** — `nature_volume` (default 0.35)
4. **Noise** — `noise_volume` (default 0.30)
5. **Binaural** — `binaural_volume` (default 0.35), skip if distance > 1.2

**Determinism note (external eval §2 #3):** `generateMix` is pure argmax — the same slider position yields the identical mix (only motif randomness differs). Candidate for weighted top-k selection; tracked in `ROADMAP.md` Phase 3 as it serves the ephemerality thesis.

**Motif density** (`_inferMotifDensity(category)`) reads Remote Config per category — a flat point value, **not** energy-scaled *(audit #6)*:

| Category | Remote Config key | Default (`main.dart`) |
|---|---|---|
| `Sleep` | `motif_density_sleep` | 0.25 |
| `Focus` | `motif_density_focus` | 0.40 |
| `Energize` | `motif_density_energize` | 0.70 |
| `Relax` | `motif_density_relax` | 0.40 |
| `Meditate` | *(none — see below)* | 0.40 |

> **`Meditate` has no dedicated density and inherits `Relax`'s** *(audit #6, ruled R-06)*. There is **no `motif_density_meditate` Remote Config key**; the switch in `_inferMotifDensity` cases only `Sleep` / `Energize` / `Focus`, so `Meditate` reaches `default:` and reads `motif_density_relax`. This is the current behavior of record — the key was deliberately **not** added, because the category taxonomy is expected to be replaced rather than retuned in V2 (D-010, `PRODUCT_DESIGN.md` §3.8). Consequence: tuning `motif_density_relax` remotely moves Meditate too; the two categories cannot be separated without a code change.

**Journey generation** (`generateJourney`) — 3 waypoints, `easeInOut` curves (weights 0 / 1.0 / 1.0). Two behaviors were absent from doc v2.4 and are **documented here as intended current behavior** *(audit #8, ruled R-08)*:

- **Waypoint 0 — baseline.** Each recommendation at its Remote Config volume; motif layer (if the palette is non-empty) at the category density, layer volume 0.3.
- **Waypoint 1 — mid-journey variation.** Each layer's volume is perturbed by **±0.05 on alternating indices** (even index `+0.05`, odd `−0.05`) and **clamped to `[0.10, 0.70]`**. Motif density is unchanged. This is a deliberate slight drift so the middle of a journey is not a static hold of waypoint 0.
- **Waypoint 2 — settle, with a Sleep taper.** Layer volumes return to their waypoint-0 values. Motif density returns to the category density **except for `Sleep`, where it drops to a hardcoded `0.1`** (`final wp2Density = category == 'Sleep' ? 0.1 : motifDensity;`) — so a sleep journey thins out toward its end rather than holding density to the finish.

> **Known Remote Config inconsistency (documented exception, not a pattern to copy).** The Sleep waypoint-2 density of `0.1` is the one motif density in the system that is **not** Remote-Config-driven — it is a literal in `generateJourney`, unreachable by remote tuning, unlike every `motif_density_*` value above. This contradicts `ENGINEERING_PRINCIPLES.md` rule 4. It is ruled intended-for-now and left in place under R-08 rather than promoted to a key, on the same reasoning as R-05/R-06 (the category system itself is slated for replacement, D-010). **Do not replicate this shape in new code**; new densities and layer volumes route through Remote Config.

### 3.5 Harmonic Matcher (`harmonic_matcher.dart`)

Pure math, no Flutter dependencies. **The project's core differentiator.**

- `frequencyToMidi` / `midiToFrequency`; `semitonesBetween` (octave-folded to ±6)
- `findBestMatch(soundscapeRootHz, targetHz)` — evaluates 7 consonant intervals `[0,3,4,5,7,9,12]`, picks the least-shift match. Octave-folding means motif root octave errors have no behavioral effect *(context for audit #11/#12)*
- `harmonicCompatibility(rootHz, solfeggioHz)` — unison/octave 1.0, P5 0.9, M3 0.8, m3 0.7, else 0.3 (feeds 40% of soundscape selection)
- `findBinauralCarrier(rootHz, solfeggioHz, {beatFrequencyHz})` *(audit #9 — the `beatFrequencyHz` param exists; doc v2.4 §4.5 had the stale signature)* — carrier on root or perfect 5th; octave-transposed into 80–300 Hz (or **200–400 Hz when `beatFrequencyHz >= 15`**), preferring the octave furthest in real semitones from the solfeggio. **The `beatFrequencyHz` argument is not currently passed from the `mood_engine.dart` call site** *(audit #28)* — so the beta/gamma range is not yet active in generated mixes.

### 3.6 Motif Engine (`motif_engine.dart`)

Eno-style generative one-shots, ~150 lines. Prime-number cycle lengths `[13,17,19,23,29,31,37,41,43]`s; per-cycle probability gating against `density`; ±20% random volume; random initial offsets; harmonic pitch-shifting of tonal motifs toward the active solfeggio (atonal motifs play unshifted). Kept outside the 5-layer cap.

> **Known behavioral concern (Robert, re-engagement):** the prime-cycle probability model produces clumpy/uneven density and makes long-cycle motifs feel like they vanish; it delivers *non-repetition* but not *steady organic density with a frequency hierarchy*. Redesign candidate — `ROADMAP.md` Phase 3.

### 3.7 LLM Service (`llm_service.dart`)

Small stateless service: strict JSON-only system prompt, 100-token cap, 10s timeout, markdown-fence stripping, null-on-any-failure with slider fallback in the UI. Used as an input parser; the app degrades cleanly without it.

### 3.8 Analytics Service (`analytics_service.dart`)

Five Firebase Analytics event wrappers, injected into `HomeScreen` and `MixerScreen`.

> ⚠ *Privacy concern (external eval §3): `logLlmGenerate` forwards the user's raw free-text mood description to Firebase Analytics. This is sensitive data for a wellness-adjacent app — log derived values/length, not the text. Pre-launch fix, `ROADMAP.md` Phase 4.*

---

## 4. Data Model (`journey.dart`, `saved_mix.dart`)

**SoundSource hierarchy:** `SampleSource`, `SoundscapeSource` (adds `pitchShiftRatio`, `rootFrequency`), `ToneSource`, `BinauralSource`, `MotifSource`. Gives the journey engine a uniform way to interpolate heterogeneous layers.

**SavedMix / MixLayer:** persists `assetPath` / `name` / `volume` only.

> ⚠ *Lossy-save concern (external eval §2 #4): pitch-shift ratios, tone/binaural frequency params, and motif state are **not** persisted. A mood-generated mix cannot faithfully round-trip; tone layers stored as semantic IDs like `tone:528` will fail if replayed through the sample-based `addLayer` path (`setAsset('tone:528')`), with no error handling in the chain. Fix before beta — `ROADMAP.md` Phase 4.*

---

## 5. Catalog (verified against code — see `CONTENT_PRODUCTION.md` for the full registry)

- **`kSoundCatalog`:** 43 entries, **35 available** (8 coming-soon noise colors are `isAvailable: false`) *(audit #16)*
- **Soundscapes:** 10, across 9 unique roots *(audit #21)* {F, D, G, Ab, A, C, E, F#, B}, A appearing twice (major + minor). Missing: Db, Eb, Bb.
- **Frequencies:** 10 — the 9 solfeggio tones **plus 432 Hz** *(audit #22)*
- **Noise:** 5 available (white, pink, brown, blue, **red** — not "yellow") *(audit #3)* + 8 coming-soon
- **Motifs (`kMotifCatalog`):** 9 — 6 tonal (bowls G3/**G5**, bells B4/**Eb3**, piano F4, **triangle E4** — *not a "gong C3"*) *(audit #11/#12/#19)* + 3 atonal. Tag discrepancies vs. doc v2.4 are open Bucket-C rulings *(audit #14/#15)*.

---

## 6. Audio Behavior Reference (empirically-tuned — do not change without a ruling)

Knowledge here is hard-won and easy to re-break. Per `ENGINEERING_PRINCIPLES.md`, changes to this behavior require explicit discussion and a Changelog entry.

- **Chrome vs. mobile:** `setSpeed()` pitch-shifting warbles on Chrome; absent on Android. Chrome is not a valid environment for judging audio quality.
- **Equal-power crossfade:** cos/sin curves; 3s window, 3.5s before loop point.
- **Binaural carrier placement:** root or 5th, 80–300 Hz (200–400 for beta/gamma), furthest-octave-from-solfeggio.
- **Mastering target for all content:** −14 LUFS, −1.0 dBTP, 44100 Hz, A=440, ±5 cents tuning tolerance. (See `CONTENT_PRODUCTION.md`.)
